/**
	Push Button [Allen-Bradley] (No Light) NL2 Script
	David Wolfe (Red-Thirten)
	07/10/2026
	Extends PushButtonAB to define custom function list & parameters
		specific to Push Buttons without a light.
	Licensed under GNU GPLv3 - See LICENSE for more details.
*/

import com.nolimitscoaster.SceneObject;

public final class PushButtonABNoLight extends PushButtonAB{
	PushButtonABNoLight(){
		SCRIPT_NAME = "PushButtonABNoLight";	// Script name for debugging
		BUTTON_TRAVEL_DIST = 0.005f;			// Distance, in meters, the button will travel to be fully "pressed"
		BUTTON_HAS_LIGHT = false;				// True if the button has a pilot light
	}

	protected final int getFuncFromScoParams(SceneObject sco){
		/* Get Function from SCO Parameters
			Returns the proper function number based on the "function" parameter enumeration.
		*/
		if (sco.getIntegerParameter("function") == null) return 0;

		int funcVal = sco.getIntegerParameter("function").intValue();
		if (funcVal > 0){
			return funcVal + 2;
		}
		return funcVal;
	}
}